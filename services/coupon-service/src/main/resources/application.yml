spring:
  application:
    name: coupon-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
  config:
    import: nacos:coupon-service?refreshEnabled=true&group=DEFAULT_GROUP
