spring:
  application:
    name: seckill-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
  config:
    import: nacos:seckill-service.yaml?refreshEnabled=true&group=DEFAULT_GROUP
