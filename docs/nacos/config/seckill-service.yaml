server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cm_seckill?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai

mybatis-plus:
  type-aliases-package: com.cloudmall.seckill.entity
